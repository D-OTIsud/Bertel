# Catalogue des notifications de modération interne — 11 septembre 2026

Capture du catalogue : **2026-09-11T15:44:29.989Z**, après exécution de `supabase/migrations/20260911152231_internal_pending_change_notifications.sql` dans une transaction terminée par **ROLLBACK**. Ce document décrit le résultat attendu de cette migration ; cette capture ne constitue pas un déploiement.

- Les huit fonctions concernées ont été relues dans `pg_catalog` : `api.list_my_notifications`, `api.claim_unmailed_notifications`, `api.mark_notifications_emailed`, `api.count_notifications_blocked_no_smtp` et les quatre nouvelles fonctions `internal.user_can_moderate_pending_object`, `internal.pending_change_notification_is_open`, `internal.sync_pending_change_notifications`, `internal.tg_pending_change_notifications`.
- Les colonnes, contraintes, index et triggers de `public.app_notification` (12 colonnes, 8 index) et `public.pending_change` (17 colonnes, 6 index, 7 triggers) ont été capturés intégralement. Le graphe représente désormais `app_notification` comme table complète : elle existait déjà en base, mais seul son squelette RLS figurait dans le snapshot précédent.
- `trg_pending_change_notifications` relie les insertions, modifications de groupe ou de statut et suppressions de propositions au cycle des alertes. Le trigger existant `trg_fiche_submission_resolve`, absent du précédent snapshot de cette table, est également présent.
- Les nouveaux index sont `uq_app_notification_pending_change_recipient`, `idx_pending_change_internal_open` et `idx_app_notification_pending_change_group`. L'index `idx_app_notification_unmailed` et la contrainte `chk_app_notification_kind` incluent désormais `pending_change_submitted`. Les index et contraintes sont conservés dans l'extraction source ; le modèle du graphe ne les expose pas comme nœuds.

Une seule alerte reste ouverte par destinataire, fiche et contributeur tant qu'une proposition interne attend une décision. Les propositions du portail (`submission_id` renseigné) gardent leur circuit propre. La résolution partielle conserve l'état lu/envoyé de l'alerte ; la dernière résolution la supprime. Une nouvelle proposition après clôture ouvre un nouvel épisode.

Les artefacts ont été régénérés avec `tools/db-graph/db_graph.py`, en ajoutant les sources de cette migration aux métadonnées des huit fonctions. Les métadonnées et l'ordre des nœuds sans rapport avec ce changement sont conservés pour limiter le diff. Aucun écart de fonction vivante hors périmètre n'a été importé.

Le reste des tables/FK conserve le snapshot historique, et les référentiels conservent l'export antérieur du 15 juin 2026 décrit dans `REFRESH_20260907.md`. Aucun export global tbls ni rafraîchissement des pages `dbdoc/` n'est revendiqué. Aucune donnée métier ni secret SMTP/Vault n'a été extrait pour cette mise à jour. Les corps des fonctions ont servi à inférer les relations, sans être publiés dans les artefacts.
