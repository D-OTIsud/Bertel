# FAQ « Aide » — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Brancher le bouton « Aide » (mort) de la sidebar sur une page `/aide` : centre d'aide interne avec recherche métier instantanée et un corpus de ~83 questions/réponses couvrant toute l'app (création par type d'objet, arbitrages, Explorer, éditeur, publication, listes, CRM, équipe, réglages).

**Spec:** `docs/superpowers/specs/2026-07-03-faq-aide-design.md` (committée `91b7be1`). Le plan la raffine sur UN point : la page vit dans `src/views/HelpPage.tsx` (convention maison : `views/XxxPage.tsx` + wrapper mince `app/(main)/xxx/page.tsx`), le contenu et la recherche dans `src/features/help/`.

**Architecture:** Contenu = modules TS typés (`FaqEntry[]` par rubrique, réponses en Markdown rendues par le `MarkdownContent` existant — XSS-safe, jamais de HTML). Recherche = fonction pure côté client (normalisation accents, préfixes, scoring keywords > question > réponse). Deep-links `?question=<id>` / `?q=<texte>`. Navigation : le bouton footer sidebar devient un `Link`, `/aide` entre dans `NAV_ITEMS` (palette ⌘K gratuite).

**Tech Stack:** Next.js App Router (client components), TypeScript strict, Jest + React Testing Library, `markdown-to-jsx` via `MarkdownContent`, CSS scopé dans `styles.css` (famille `.help-*`, pattern `.crm-app`).

## Global Constraints

- **Cwd de toutes les commandes :** `C:\Users\dphil\Bertel3.0\bertel-tourism-ui` (sauf mention contraire).
- **Commandes :** tests `npm run test:run -- <pattern>` ; typecheck `npm run typecheck`.
- **Commits :** conventionnels, en français, **SANS trailer Co-Authored-By** (préférence utilisateur, attribution désactivée). Stage par pathspec + commit dans la MÊME invocation (une session parallèle peut balayer l'index partagé). Ne jamais `git push`.
- **Règle d'or du contenu :** chaque affirmation d'une réponse décrit le **comportement réel vérifié** dans les sources listées (code, decision log, docs). Si un fait n'est pas vérifiable → on ne l'écrit pas. Aucune fonctionnalité non livrée ne doit être décrite.
- **Markdown des réponses :** sous-ensemble `MarkdownContent` uniquement — gras `**`, italique `*`, listes `-`/`1.`, citation `>`, liens `https:`/`mailto:` (rares). **PAS de HTML brut, PAS de titres H1-H6** (les réponses sont compactes : paragraphes à étiquette en gras `**C'est quoi.**`). Les renvois internes passent par le champ `related` (les liens relatifs seraient neutralisés par `SafeLink`), jamais par un lien Markdown.
- **Voix du contenu :** « vous », phrases courtes, 5–15 lignes de Markdown par réponse, vocabulaire conseiller (« fiche », pas « objet » ; « gîte », pas « HLO » — les codes apparaissent entre parenthèses à la première mention).
- **Keywords :** ≥ 3 par entrée, vocabulaire MÉTIER réellement tapé par un agent (« gîte », « atelier », « marché »…) ; les accents sont autorisés (la recherche normalise les deux côtés) ; inclure les synonymes locaux (« kaz », « rando »…).
- **Immutabilité, pas de `console.log`, pas de `any`.**

## Sources de vérification du contenu (la « bible »)

| Source | Chemin | Sert à |
|---|---|---|
| Decision log canonique | `bertel-tourism-ui/claude_brief/lot1_mapping_decisions.md` | tout comportement §-numéroté cité ci-dessous |
| Analyse typologique | `docs/research/type-gap-analysis-2026-06-11.md` | définitions de types, arbitrages, règle marchés |
| Guide partenaires | `docs/guide-partenaires.md` | formulations grand-public déjà validées |
| Invariants | `CLAUDE.md` (racine) | ORG vs ACTOR, pattern d'attachement ACT, cycle de vie |
| Création | `src/features/object-editor/create/CreateObjectDialog.tsx` + `create-object-options.ts` | flux réel de création |
| Types/archétypes | `src/features/object-editor/archetypes.ts` | les 18 types, libellés, archétypes |
| Éditeur | `src/features/object-editor/sections/` (registre des 22 sections) | noms réels des sections |
| Explorer | `src/features/explorer/` + `src/hooks/useExplorerUrlSync.ts` | filtres, vues, ?fiche= |
| Équipe | `src/views/TeamAdminPage.tsx` + mémoire §164 | invitations, rôles |

**Avant de rédiger une entrée, ouvrir la source indiquée dans sa ligne du tableau des entrées.** En cas de doute sur un libellé exact d'UI (nom de bouton, de section), grepper le composant.

---

### Task 1 : Modèle de contenu + registre des rubriques

**Files:**
- Create: `src/features/help/content/types.ts`
- Test: `src/features/help/content/types.test.ts`

**Interfaces:**
- Consumes: rien.
- Produces: `FaqEntry` (interface), `FaqRubriqueId` (union), `FAQ_RUBRIQUES: readonly {id, label}[]` — consommés par TOUTES les tâches suivantes.

- [ ] **Step 1: Écrire le test qui échoue**

```ts
// src/features/help/content/types.test.ts
import { FAQ_RUBRIQUES } from './types';

describe('registre des rubriques FAQ', () => {
  test('ids uniques, slug-safe, libellés non vides', () => {
    const ids = FAQ_RUBRIQUES.map((r) => r.id);
    expect(new Set(ids).size).toBe(ids.length);
    for (const r of FAQ_RUBRIQUES) {
      expect(r.id).toMatch(/^[a-z0-9-]+$/);
      expect(r.label.trim().length).toBeGreaterThan(0);
    }
  });

  test('les 10 rubriques de la spec, dans l’ordre de lecture', () => {
    expect(FAQ_RUBRIQUES.map((r) => r.id)).toEqual([
      'demarrer', 'creer-objet', 'choisir-type', 'explorer', 'editeur',
      'publication', 'listes', 'crm', 'equipe', 'reglages',
    ]);
  });
});
```

- [ ] **Step 2: Vérifier l'échec** — `npm run test:run -- features/help` → FAIL (module `./types` introuvable).

- [ ] **Step 3: Implémenter**

```ts
// src/features/help/content/types.ts
/**
 * FAQ « Aide » (spec docs/superpowers/specs/2026-07-03-faq-aide-design.md).
 * Le contenu vit dans le repo : chaque rubrique = un module TS exportant des FaqEntry[].
 * Réponses en Markdown, sous-ensemble MarkdownContent (gras/italique/listes/citation/liens
 * https|mailto) — JAMAIS de HTML brut, pas de titres. Renvois internes via `related`
 * (un lien Markdown relatif serait neutralisé par SafeLink), rendus par la page.
 */

export const FAQ_RUBRIQUES = [
  { id: 'demarrer', label: 'Démarrer & compte' },
  { id: 'creer-objet', label: 'Créer une fiche' },
  { id: 'choisir-type', label: 'Choisir le bon type' },
  { id: 'explorer', label: 'Explorer & filtres' },
  { id: 'editeur', label: 'Éditer une fiche' },
  { id: 'publication', label: 'Publication & modération' },
  { id: 'listes', label: 'Listes & impression' },
  { id: 'crm', label: 'CRM' },
  { id: 'equipe', label: 'Équipe & rôles' },
  { id: 'reglages', label: 'Réglages & RGPD' },
] as const;

export type FaqRubriqueId = (typeof FAQ_RUBRIQUES)[number]['id'];

export interface FaqEntry {
  /** Slug stable et unique — ancre du deep-link `?question=<id>`. Format ^[a-z0-9-]+$. */
  id: string;
  rubrique: FaqRubriqueId;
  /** Formulation « voix de l'agent » (ex. « Je veux créer un artisan… »). */
  question: string;
  /** Markdown (sous-ensemble MarkdownContent). Compact : 5–15 lignes. */
  answer: string;
  /** Vocabulaire MÉTIER tapé réellement (« gîte », « atelier »…) — poids fort en recherche. */
  keywords: string[];
  /** Codes object_type concernés — badges UI + invariant de couverture TYPE_ARCHETYPES. */
  types?: string[];
  /** « Voir aussi » : ids d'autres entrées, rendus par la page comme boutons. */
  related?: string[];
}
```

- [ ] **Step 4: Vérifier le vert** — `npm run test:run -- features/help` → PASS (2 tests).

- [ ] **Step 5: Commit**

```bash
git add src/features/help/content/types.ts src/features/help/content/types.test.ts
git commit -m "feat(aide): modele FaqEntry + registre des 10 rubriques du centre d'aide" -- src/features/help/content/types.ts src/features/help/content/types.test.ts
```

---

### Task 2 : Recherche pure `searchFaq`

**Files:**
- Create: `src/features/help/faq-search.ts`
- Test: `src/features/help/faq-search.test.ts`

**Interfaces:**
- Consumes: `FaqEntry`, `FAQ_RUBRIQUES` (Task 1).
- Produces: `normalizeFaqText(value: string): string` et `searchFaq(entries: readonly FaqEntry[], query: string): FaqEntry[]` — consommés par `HelpPage` (Task 8) et le test corpus (Task 7).

- [ ] **Step 1: Écrire les tests qui échouent**

```ts
// src/features/help/faq-search.test.ts
import type { FaqEntry } from './content/types';
import { normalizeFaqText, searchFaq } from './faq-search';

function entry(partial: Partial<FaqEntry> & Pick<FaqEntry, 'id'>): FaqEntry {
  return {
    rubrique: 'demarrer',
    question: partial.id,
    answer: 'réponse',
    keywords: ['x-key'],
    ...partial,
  };
}

describe('normalizeFaqText', () => {
  test('minuscules + accents strippés', () => {
    expect(normalizeFaqText('Gîte à l’Étang-Salé')).toBe('gite a l’etang-sale');
  });
});

describe('searchFaq', () => {
  const corpus: FaqEntry[] = [
    entry({ id: 'a-keyword', keywords: ['artisan', 'boutique'], answer: 'rien ici' }),
    entry({ id: 'b-question', question: 'Créer un artisan ?', keywords: ['zzz'], answer: 'rien' }),
    entry({ id: 'c-answer', keywords: ['zzz'], answer: 'un artisan qui vend' }),
    entry({ id: 'd-nomatch', keywords: ['zzz'], answer: 'rien', question: 'rien' }),
  ];

  test('requête vide ou blanche → []', () => {
    expect(searchFaq(corpus, '')).toEqual([]);
    expect(searchFaq(corpus, '   ')).toEqual([]);
  });

  test('préfixe : « artis » matche artisan ; « d-nomatch » exclu', () => {
    const ids = searchFaq(corpus, 'artis').map((e) => e.id);
    expect(ids).toContain('a-keyword');
    expect(ids).not.toContain('d-nomatch');
  });

  test('pondération : keyword > question > answer', () => {
    expect(searchFaq(corpus, 'artisan').map((e) => e.id)).toEqual([
      'a-keyword', 'b-question', 'c-answer',
    ]);
  });

  test('insensible aux accents dans les deux sens', () => {
    const c = [entry({ id: 'gite', keywords: ['gîte'] })];
    expect(searchFaq(c, 'gite').map((e) => e.id)).toEqual(['gite']);
    expect(searchFaq(c, 'GÎTE').map((e) => e.id)).toEqual(['gite']);
  });

  test('multi-tokens = ET : chaque token doit matcher quelque part', () => {
    const c = [
      entry({ id: 'both', keywords: ['artisan'], answer: 'propose des ateliers' }),
      entry({ id: 'one', keywords: ['artisan'], answer: 'rien' }),
    ];
    expect(searchFaq(c, 'artisan atelier').map((e) => e.id)).toEqual(['both']);
  });

  test('les codes types matchent aussi (« hlo »)', () => {
    const c = [entry({ id: 'hlo', types: ['HLO'], keywords: ['gîte'] })];
    expect(searchFaq(c, 'hlo').map((e) => e.id)).toEqual(['hlo']);
  });

  test('départage stable à score égal : ordre rubrique puis id', () => {
    const c = [
      entry({ id: 'z-late', rubrique: 'crm', keywords: ['artisan'] }),
      entry({ id: 'a-early', rubrique: 'demarrer', keywords: ['artisan'] }),
    ];
    expect(searchFaq(c, 'artisan').map((e) => e.id)).toEqual(['a-early', 'z-late']);
  });
});
```

- [ ] **Step 2: Vérifier l'échec** — `npm run test:run -- faq-search` → FAIL (module inexistant).

- [ ] **Step 3: Implémenter**

```ts
// src/features/help/faq-search.ts
/**
 * Recherche du centre d'aide — pure, côté client (spec 2026-07-03-faq-aide-design.md).
 * Exigences PO : simple et efficace — insensible aux accents, correspondance par PRÉFIXE
 * (« artis » → artisan/artisanat), et le vocabulaire métier (keywords) pèse plus lourd
 * que la question, elle-même plus lourde que le corps de la réponse. Multi-tokens = ET.
 * ponytail: scan linéaire à chaque frappe (~83 entrées) ; si le corpus dépasse ~500
 * entrées, précalculer les mots par entrée dans un Map module-level.
 */
import { FAQ_RUBRIQUES, type FaqEntry } from './content/types';

const SCORE_KEYWORD = 8;
const SCORE_QUESTION = 4;
const SCORE_ANSWER = 1;

/** Minuscules + diacritiques strippés (NFD) — appliqué aux deux côtés de la recherche. */
export function normalizeFaqText(value: string): string {
  return value.toLowerCase().normalize('NFD').replace(/[̀-ͯ]/g, '');
}

function toWords(value: string): string[] {
  return normalizeFaqText(value).split(/[^a-z0-9]+/).filter(Boolean);
}

/** Un token matche un champ s'il est préfixe d'un de ses mots. */
function fieldScore(fieldWords: readonly string[], token: string, weight: number): number {
  return fieldWords.some((word) => word.startsWith(token)) ? weight : 0;
}

const RUBRIQUE_ORDER = new Map(FAQ_RUBRIQUES.map((r, index) => [r.id, index]));

export function searchFaq(entries: readonly FaqEntry[], query: string): FaqEntry[] {
  const tokens = toWords(query);
  if (tokens.length === 0) return [];

  const scored: Array<{ entry: FaqEntry; score: number }> = [];
  for (const entry of entries) {
    const keywordWords = entry.keywords.flatMap(toWords)
      .concat((entry.types ?? []).map(normalizeFaqText));
    const questionWords = toWords(entry.question);
    const answerWords = toWords(entry.answer);

    let total = 0;
    let allTokensMatch = true;
    for (const token of tokens) {
      const score =
        fieldScore(keywordWords, token, SCORE_KEYWORD) +
        fieldScore(questionWords, token, SCORE_QUESTION) +
        fieldScore(answerWords, token, SCORE_ANSWER);
      if (score === 0) {
        allTokensMatch = false;
        break;
      }
      total += score;
    }
    if (allTokensMatch) scored.push({ entry, score: total });
  }

  return scored
    .sort(
      (a, b) =>
        b.score - a.score ||
        (RUBRIQUE_ORDER.get(a.entry.rubrique) ?? 0) - (RUBRIQUE_ORDER.get(b.entry.rubrique) ?? 0) ||
        a.entry.id.localeCompare(b.entry.id),
    )
    .map((item) => item.entry);
}
```

- [ ] **Step 4: Vérifier le vert** — `npm run test:run -- faq-search` → PASS (8 tests).

- [ ] **Step 5: Commit**

```bash
git add src/features/help/faq-search.ts src/features/help/faq-search.test.ts
git commit -m "feat(aide): recherche pure searchFaq — accents strippes, prefixes, scoring keywords>question>reponse, multi-tokens ET" -- src/features/help/faq-search.ts src/features/help/faq-search.test.ts
```

---

### Task 3 : Corpus « Créer une fiche » (19 entrées) + index + test d'intégrité

**Files:**
- Create: `src/features/help/content/creer-objet.ts`
- Create: `src/features/help/content/index.ts`
- Test: `src/features/help/content-integrity.test.ts`

**Interfaces:**
- Consumes: `FaqEntry` (Task 1), `TYPE_ARCHETYPES` de `src/features/object-editor/archetypes.ts`.
- Produces: `CREER_OBJET_FAQ: FaqEntry[]` ; `ALL_FAQ_ENTRIES: FaqEntry[]` (dans `content/index.ts` — **chaque tâche de contenu suivante ajoute son import ici**).

- [ ] **Step 1: Écrire le test d'intégrité (échoue : contenu absent)**

```ts
// src/features/help/content-integrity.test.ts
/**
 * Invariant du centre d'aide : un 19e type d'objet ne peut PAS arriver sans sa fiche
 * d'aide (couverture TYPE_ARCHETYPES), un id ne peut pas être dupliqué, un renvoi
 * `related` ne peut pas pointer dans le vide, une réponse ne peut pas embarquer de HTML.
 */
import { TYPE_ARCHETYPES } from '../object-editor/archetypes';
import { ALL_FAQ_ENTRIES } from './content';
import { FAQ_RUBRIQUES } from './content/types';

describe('intégrité du contenu FAQ', () => {
  const ids = ALL_FAQ_ENTRIES.map((e) => e.id);

  test('au moins 19 entrées (corpus création)', () => {
    expect(ALL_FAQ_ENTRIES.length).toBeGreaterThanOrEqual(19);
  });

  test('ids uniques et slug-safe', () => {
    expect(new Set(ids).size).toBe(ids.length);
    for (const id of ids) expect(id).toMatch(/^[a-z0-9-]+$/);
  });

  test('rubrique valide, question/réponse non vides, ≥3 keywords', () => {
    const valid = new Set<string>(FAQ_RUBRIQUES.map((r) => r.id));
    for (const e of ALL_FAQ_ENTRIES) {
      expect(valid.has(e.rubrique)).toBe(true);
      expect(e.question.trim().length).toBeGreaterThan(0);
      expect(e.answer.trim().length).toBeGreaterThan(0);
      expect(e.keywords.length).toBeGreaterThanOrEqual(3);
    }
  });

  test('chaque type de TYPE_ARCHETYPES est couvert par au moins une entrée', () => {
    const covered = new Set(ALL_FAQ_ENTRIES.flatMap((e) => e.types ?? []));
    for (const code of Object.keys(TYPE_ARCHETYPES)) expect(covered).toContain(code);
  });

  test('les types déclarés sont des codes connus', () => {
    const known = new Set(Object.keys(TYPE_ARCHETYPES));
    for (const e of ALL_FAQ_ENTRIES) {
      for (const t of e.types ?? []) expect(known.has(t)).toBe(true);
    }
  });

  test('related pointe vers des entrées existantes, jamais soi-même', () => {
    const all = new Set(ids);
    for (const e of ALL_FAQ_ENTRIES) {
      for (const r of e.related ?? []) {
        expect(all.has(r)).toBe(true);
        expect(r).not.toBe(e.id);
      }
    }
  });

  test('pas de HTML brut ni de titres Markdown dans les réponses', () => {
    for (const e of ALL_FAQ_ENTRIES) {
      expect(e.answer).not.toMatch(/<[a-z][^>]*>/i);
      expect(e.answer).not.toMatch(/^#{1,6} /m);
    }
  });
});
```

- [ ] **Step 2: Vérifier l'échec** — `npm run test:run -- content-integrity` → FAIL (`./content` introuvable).

- [ ] **Step 3: Rédiger `creer-objet.ts` (19 entrées)**

Structure du fichier :

```ts
// src/features/help/content/creer-objet.ts
/** Rubrique « Créer une fiche » — 1 entrée générale + 1 fiche par type (les 18 de
 *  TYPE_ARCHETYPES = enum object_type moins ORG). Gabarit fixe (spec) :
 *  **C'est quoi.** / **Quand choisir ce type.** / **Sous-catégorie.** / **Étapes.** / **Pièges.** */
import type { FaqEntry } from './types';

export const CREER_OBJET_FAQ: FaqEntry[] = [ /* …19 entrées… */ ];
```

**Entrée générale, à écrire telle quelle** (vérifiée sur `CreateObjectDialog.tsx` — la garde `canCreateObjects`, le picker groupé par archétype, la redirection éditeur) :

```ts
{
  id: 'creer-fiche',
  rubrique: 'creer-objet',
  question: 'Comment créer une nouvelle fiche ?',
  keywords: ['créer', 'nouvelle fiche', 'ajouter', 'établissement', 'nouveau'],
  // related inter-rubriques ajoutés quand leurs cibles existent : 'choisir-artisan' (Task 4),
  // 'publier-fiche' (Task 6) — le test d'intégrité interdit un renvoi vers un id absent.
  answer: `**Où.** Bouton **Créer** de l'Explorer, ou palette de commandes (Ctrl/⌘ + K → « Créer une fiche »). Si le bouton n'apparaît pas, votre compte n'a pas le droit de création — voyez l'administrateur de votre organisation.

**Comment.** 1. Choisissez le **type de fiche** (regroupés par famille : hébergement, restaurant, activité, itinéraire, visite, service, événement) — ce choix conditionne les sections de l'éditeur, prenez le temps de vérifier l'arbitrage dans la rubrique « Choisir le bon type ». 2. Saisissez le **nom exact** de l'établissement ou de l'offre. 3. La fiche s'ouvre en **brouillon** dans l'éditeur.

**Ensuite.** Remplissez les sections prioritaires (identité, localisation, contacts, description, photos) puis publiez depuis la section Publication. Un brouillon n'est visible que par votre organisation.`,
},
```

**Exemplaire du gabarit type, à écrire tel quel** (HLO ; vérifier le libellé picker « Gîte & meublé » dans `create-object-options.ts` / `TYPE_LABEL`) :

```ts
{
  id: 'creer-hlo',
  rubrique: 'creer-objet',
  question: 'Comment créer un gîte ou un meublé de tourisme ?',
  keywords: ['gîte', 'meublé', 'location saisonnière', 'kaz', 'villa', 'bungalow', 'location vacances'],
  types: ['HLO'],
  related: ['creer-fiche'], // + 'choisir-hlo-rva' ajouté en Task 4 (cible pas encore créée ici)
  answer: `**C'est quoi.** Un logement entier loué à la semaine ou à la nuitée (gîte, meublé, villa, bungalow) — type **Gîte & meublé (HLO)**. C'est le type le plus courant du territoire.

**Quand choisir ce type.** Le voyageur loue le logement complet, sans services hôteliers. **Pas ce type si** : chambres à la nuitée avec réception/services → Hôtel (HOT) ; ensemble d'appartements avec services collectifs → Résidence de vacances (RVA) ; emplacements de plein air → Camping (CAMP) ou Hôtellerie de plein air (HPA).

**Étapes.** Créer → type « Gîte & meublé » → nom exact. Dans l'éditeur, renseignez en priorité : localisation, contacts, description, photos, la **capacité** (section Chambres, capacité & séminaire) et les tarifs.

**Pièges.** Le classement en étoiles (meublé de tourisme) se saisit dans Classement & labels — jamais dans le nom de la fiche.`,
},
```

**Les 17 autres fiches suivent le MÊME gabarit.** Tableau de rédaction (id · question · points imposés de la réponse · keywords minimaux) — pour chaque ligne, vérifier libellé et périmètre dans `archetypes.ts` (`TYPE_LABEL`, `ArchetypeMeta.family/covers`) et `type-gap-analysis-2026-06-11.md` :

| id | types | Question (« Comment créer… ») | Points imposés | Keywords minimaux |
|---|---|---|---|---|
| `creer-hot` | HOT | …un hôtel ? | services hôteliers à la nuitée ; vs HLO (logement entier) et RVA ; capacité/chambres §06 ; classement étoiles §08 | hôtel, chambre, nuitée, étoiles |
| `creer-hpa` | HPA | …une hôtellerie de plein air ? | hébergement plein air aménagé ; vs CAMP ; emplacements/capacité | plein air, mobil-home, emplacement, glamping |
| `creer-camp` | CAMP | …un camping ? | emplacements tentes/caravanes ; vs HPA ; capacité | camping, tente, caravane, emplacement |
| `creer-rva` | RVA | …une résidence de vacances ? | ensemble locatif avec services collectifs ; vs HLO (voir `choisir-hlo-rva`) | résidence, appartement, studio, vacances |
| `creer-res` | RES | …un restaurant, un bar, un snack ? | famille « Restauration · Bar · Snack » ; cuisine + menus structurés + carte PDF (§104) ; horaires §14 | restaurant, snack, bar, table, carte, menu |
| `creer-iti` | ITI | …un itinéraire de randonnée ? | tracé GPX importé (pas d'édition manuelle du tracé — honnêteté §48) ; étapes + photos d'étape ; publication bloquée sans géométrie | randonnée, rando, sentier, boucle, trail, vtt, itinéraire |
| `creer-fma` | FMA | …une fête ou une manifestation ? | événement DATÉ (dates/occurrences) ; vs ACT récurrente (renvoi `choisir-fma-act`) | fête, événement, manifestation, festival, concert |
| `creer-asc` | ASC | …une activité sportive ou culturelle ? | activité pratiquée en autonomie ou encadrée légère ; vs ACT (renvoi `choisir-asc-act`) | activité, sport, culture, initiation |
| `creer-act` | ACT | …une activité encadrée (atelier, sortie guidée) ? | prestation commerciale encadrée ; **pattern d'attachement CLAUDE.md** : publisher ORG + acteur opérateur + lieu de RDV (object_location) ; renvoi `choisir-artisan`, `choisir-org-actor` | atelier, stage, guide, sortie, encadrée, réservation |
| `creer-loi` | LOI | …un loisir (parc, plaine de jeux…) ? | équipement de loisir permanent ; vs PCU/PNA (renvois) | loisir, parc, jeux, accrobranche |
| `creer-pcu` | PCU | …un site patrimonial ou culturel ? | patrimoine bâti/culturel, musée ; vs LOI (renvoi `choisir-pcu-loi`) | patrimoine, musée, église, culture, monument |
| `creer-pna` | PNA | …un site naturel ? | site naturel remarquable (cascade, point de vue…) ; vs LOI (renvoi `choisir-pna-loi`) | nature, cascade, point de vue, forêt, site naturel |
| `creer-prd` | PRD | …un producteur ? | production locale visitable/vente directe (§57) ; vs COM revendeur (renvoi `choisir-prd-com`) | producteur, ferme, vanille, distillerie, dégustation |
| `creer-psv` | PSV | …un prestataire de services ? | prestataire touristique hors activité encadrée (transport, location de matériel…) — vérifier périmètre exact dans type-gap-analysis avant rédaction | prestataire, location, transport, service |
| `creer-vil` | VIL | …une ville ou un village ? | fiche territoire/commune ; usage éditorial | ville, village, commune, bourg |
| `creer-com` | COM | …un commerce ? | commerce utile au tourisme (dont **artisanat** — renvoi `choisir-artisan`) ; sous-catégorie taxonomie | commerce, boutique, magasin, artisanat, souvenir |
| `creer-spu` | SPU | …un service public ? | service public utile au visiteur (office, poste, santé — périmètre élargi §57) | service public, mairie, office, santé |

Contraintes de rédaction communes : chaque entrée = gabarit 5 blocs ; `related` contient TOUJOURS `creer-fiche` ou l'arbitrage pertinent ; **vérifier chaque libellé de section d'éditeur cité** (grep dans `src/features/object-editor/sections/`) ; ne citer une section que si elle existe pour ce type.

- [ ] **Step 4: Créer `content/index.ts`**

```ts
// src/features/help/content/index.ts
/** Concaténation de TOUTES les rubriques — chaque tâche de contenu ajoute son import.
 *  L'ordre suit FAQ_RUBRIQUES (affichage) ; l'intégrité est gardée par content-integrity.test. */
import type { FaqEntry } from './types';
import { CREER_OBJET_FAQ } from './creer-objet';

export const ALL_FAQ_ENTRIES: FaqEntry[] = [
  ...CREER_OBJET_FAQ,
];
```

- [ ] **Step 5: Vérifier le vert** — `npm run test:run -- content-integrity` → PASS. NOTE : le test `related pointe vers des entrées existantes` échouera tant que les renvois visent des entrées d'autres rubriques pas encore écrites (`choisir-artisan`…). **Décision : dans CETTE tâche, les `related` inter-rubriques sont laissés dans le code mais le corpus choisir-type n'existe pas encore → pour rester vert, ne mettre dans `related` QUE des ids déjà présents (`creer-fiche`, `creer-*`) et AJOUTER les renvois inter-rubriques dans la Task 4 (une ligne par entrée).**

- [ ] **Step 6: Commit**

```bash
git add src/features/help/content/creer-objet.ts src/features/help/content/index.ts src/features/help/content-integrity.test.ts
git commit -m "feat(aide): corpus Creer une fiche — 19 entrees gabarit fixe (18 types TYPE_ARCHETYPES + generale) + test d'integrite (couverture types, ids uniques, related valides, pas de HTML)" -- src/features/help/content/creer-objet.ts src/features/help/content/index.ts src/features/help/content-integrity.test.ts
```

---

### Task 4 : Corpus « Choisir le bon type » (9 arbitrages)

**Files:**
- Create: `src/features/help/content/choisir-type.ts`
- Modify: `src/features/help/content/index.ts` (ajouter l'import + spread)
- Modify: `src/features/help/content/creer-objet.ts` (compléter les `related` inter-rubriques listés en Task 3)

**Interfaces:**
- Consumes: `FaqEntry` (Task 1).
- Produces: `CHOISIR_TYPE_FAQ: FaqEntry[]` ; les ids `choisir-artisan`, `choisir-hlo-rva`, `choisir-pcu-loi`, `choisir-pna-loi`, `choisir-fma-act`, `choisir-asc-act`, `choisir-prd-com`, `choisir-marche`, `choisir-org-actor` (référencés par Task 3 et Task 8).

- [ ] **Step 1: Rédiger l'entrée phare « artisan », à écrire telle quelle** (vérifier au préalable : pattern ACT dans `CLAUDE.md` § Standard ACT attachment ; sous-catégorie artisanat du domaine COM — grepper les seeds taxonomy COM dans `Base de donnée DLL et API/seeds_data.sql` ou vérifier dans l'éditeur §02 ; ajuster UNIQUEMENT le nom exact de la sous-catégorie si différent) :

```ts
{
  id: 'choisir-artisan',
  rubrique: 'choisir-type',
  question: 'Je veux créer un artisan : quel type de fiche choisir ?',
  keywords: ['artisan', 'artisanat', 'atelier', 'créateur', 'boutique', 'savoir-faire', 'métier d’art', 'démonstration', 'stage', 'fabrication'],
  types: ['COM', 'ACT', 'PRD'],
  related: ['creer-com', 'creer-act', 'creer-prd', 'choisir-org-actor'],
  answer: `Le bon type dépend de **ce que le visiteur vient faire** chez l'artisan :

- **Il achète, c'est tout** (boutique, échoppe, stand permanent) → **Commerce (COM)**, sous-catégorie artisanat. Peu importe que l'artisan produise lui-même ou revende : tant que l'offre touristique est la *vente*, c'est un commerce.
- **Il participe à un atelier, un stage ou une démonstration encadrée** (sur réservation, avec un animateur) → **Activité encadrée (ACT)**. La fiche ACT porte la *prestation* ; l'artisan qui l'anime est un **acteur** (opérateur) rattaché à la fiche — on ne crée jamais d'organisation pour lui.
- **Il visite une exploitation qui produit sur place** (vanilleraie, distillerie, torréfacteur… avec vente directe) → **Producteur (PRD)**.
- **Les deux** (boutique à l'année + ateliers réguliers) → **deux fiches liées** : un COM pour la boutique, une ACT pour l'atelier, reliées dans la section Relations de l'éditeur.

**Piège.** Ne classez pas un artisan en ACT « parce qu'il fabrique » : la fabrication n'est une activité touristique que si le visiteur peut y participer ou y assister dans un cadre organisé.`,
},
```

- [ ] **Step 2: Rédiger les 8 autres arbitrages.** Tableau de rédaction — chaque ligne DOIT être vérifiée contre `type-gap-analysis-2026-06-11.md` (« Refus motivés », règles d'arbitrage) et le decision log §57 avant rédaction :

| id | types | Question | Points imposés | Keywords minimaux |
|---|---|---|---|---|
| `choisir-hlo-rva` | HLO, RVA | Gîte, meublé ou résidence de vacances ? | logement entier isolé → HLO ; ensemble avec services collectifs → RVA ; cas réel du doublon HLO→RVA (passe 3 import) | gîte, meublé, résidence, appartement |
| `choisir-pcu-loi` | PCU, LOI | Musée, site culturel : Patrimoine ou Loisir ? | valeur patrimoniale/culturelle → PCU ; équipement récréatif → LOI | musée, patrimoine, loisir, visite |
| `choisir-pna-loi` | PNA, LOI | Cascade, point de vue, parc : Site naturel ou Loisir ? | milieu naturel → PNA ; aménagement récréatif payant → LOI | cascade, nature, parc, point de vue |
| `choisir-fma-act` | FMA, ACT | Événement ponctuel ou activité récurrente ? | daté/occurrences → FMA ; prestation réservable à l'année → ACT | événement, festival, dates, récurrent |
| `choisir-asc-act` | ASC, ACT | Activité libre (ASC) ou encadrée (ACT) ? | pratique autonome/spot → ASC ; prestation commerciale encadrée avec opérateur → ACT (mêmes sections éditeur — archétype commun) | activité, encadré, autonomie, spot |
| `choisir-prd-com` | PRD, COM | Producteur ou commerce ? | produit sur place ce qu'il vend → PRD ; revend → COM ; cas mixte = ce qui domine l'offre visiteur | producteur, revendeur, vente directe, boutique |
| `choisir-marche` | COM, FMA | Un marché forain : quel type ? | règle marchés du §57 (type-gap-analysis / dictionnaire) — VÉRIFIER la règle exacte avant rédaction, ne rien inventer | marché, forain, marché couvert, étals |
| `choisir-org-actor` | ACT | Organisation ou acteur : qui est qui ? | invariant CLAUDE.md : ORG = structure institutionnelle/éditrice ; ACTOR = opérateur/contact commercial ; ne JAMAIS créer une ORG pour un prestataire ; les acteurs se gèrent depuis le CRM et la section Prestataires | organisation, acteur, opérateur, prestataire, structure |
| `choisir-doublon` | — | L'établissement existe peut-être déjà : comment vérifier ? | chercher AVANT de créer (recherche globale Explorer/palette) ; le dialogue de création affiche les homonymes (vérifier dans `CreateObjectDialog.tsx` — sinon retirer cette phrase) ; si doublon publié repéré → signaler à l'OTI | doublon, existe déjà, duplicate, vérifier |

- [ ] **Step 3: Brancher dans l'index + compléter les related de Task 3**

Dans `content/index.ts` : ajouter `import { CHOISIR_TYPE_FAQ } from './choisir-type';` et `...CHOISIR_TYPE_FAQ` (après CREER_OBJET_FAQ). Dans `creer-objet.ts` : ajouter les renvois inter-rubriques du tableau Task 3 (`creer-fiche` → `choisir-artisan`, `creer-hlo` → `choisir-hlo-rva`, `creer-act` → `choisir-artisan` + `choisir-org-actor`, `creer-com` → `choisir-artisan`, `creer-prd` → `choisir-prd-com`, `creer-fma` → `choisir-fma-act`, `creer-asc` → `choisir-asc-act`, `creer-pcu` → `choisir-pcu-loi`, `creer-pna` → `choisir-pna-loi`).

- [ ] **Step 4: Vérifier le vert** — `npm run test:run -- content-integrity` → PASS (related désormais résolus, 28 entrées).

- [ ] **Step 5: Commit**

```bash
git add src/features/help/content/choisir-type.ts src/features/help/content/index.ts src/features/help/content/creer-objet.ts
git commit -m "feat(aide): corpus Choisir le bon type — 9 arbitrages en arbre de decision (artisan COM/ACT/PRD, HLO vs RVA, marches, ORG vs acteur...) + renvois croises depuis les fiches creation" -- src/features/help/content/choisir-type.ts src/features/help/content/index.ts src/features/help/content/creer-objet.ts
```

---

### Task 5 : Corpus « Démarrer » (7) + « Explorer » (8)

**Files:**
- Create: `src/features/help/content/demarrer.ts` (export `DEMARRER_FAQ`)
- Create: `src/features/help/content/explorer.ts` (export `EXPLORER_FAQ`)
- Modify: `src/features/help/content/index.ts`

**Interfaces:** mêmes conventions que Tasks 3-4 ; produit les ids listés ci-dessous.

- [ ] **Step 1: Rédiger `demarrer.ts`.** Exemplaire à écrire tel quel (vérifié sur le flux §170, `LoginPage.tsx`) :

```ts
{
  id: 'mot-de-passe-oublie',
  rubrique: 'demarrer',
  question: 'J’ai oublié mon mot de passe, comment le réinitialiser ?',
  keywords: ['mot de passe', 'oublié', 'réinitialiser', 'connexion', 'password'],
  related: ['connexion', 'invitation-equipe'],
  answer: `Sur l'écran de connexion, cliquez sur **« Mot de passe oublié ? »** sous le champ mot de passe, saisissez votre adresse e-mail professionnelle et validez. Si un compte existe avec cette adresse, vous recevez un e-mail avec un lien : il vous amène sur une page où définir un nouveau mot de passe.

**Le lien a expiré ?** Recommencez simplement la procédure depuis l'écran de connexion. Si l'e-mail n'arrive pas, vérifiez vos indésirables puis voyez l'administrateur de votre organisation.`,
},
```

Tableau des 6 autres (vérifier chaque ligne dans sa source) :

| id | Question | Points imposés | Source | Keywords |
|---|---|---|---|---|
| `connexion` | Comment me connecter à Bertel ? | e-mail + mot de passe sur /login ; compte créé par invitation uniquement (pas d'auto-inscription) | `LoginPage.tsx`, §164 | connexion, se connecter, login, compte |
| `invitation-equipe` | J'ai reçu une invitation par e-mail, que faire ? | lien → page de définition du mot de passe → accès à l'app ; expiré → demander un renvoi à l'admin | §164, `SetPasswordPage.tsx` | invitation, e-mail, bienvenue, activer |
| `profil-avatar` | Changer mon nom affiché ou ma photo ? | Réglages → Mon compte → Profil (nom + avatar) | §149, `SettingsPage.tsx` | profil, photo, avatar, nom |
| `roles-vue-modules` | Pourquoi certains modules me sont invisibles ? | menus filtrés par rôle (owner / super_admin / tourism_agent) ; voir l'admin d'organisation | `nav-items.ts` | rôle, accès, module, menu, invisible |
| `palette-commandes` | Aller plus vite : la palette de commandes | Ctrl/⌘+K : naviguer, chercher une fiche, créer ; raccourcis listés dans la feuille de raccourcis | `CommandPalette.tsx` | raccourci, palette, clavier, rapide |
| `session-invalide` | « Session invalide » ou déconnexion inattendue | écran brandé avec Réessayer / Aller à la connexion ; se reconnecter suffit en général | §169/§165 | session, invalide, déconnecté, erreur |

- [ ] **Step 2: Rédiger `explorer.ts`.** Tableau (sources : `src/features/explorer/`, mémoire §142/§133/§109/§161/§148, CLAUDE.md § Explorer non-published) :

| id | Question | Points imposés | Keywords |
|---|---|---|---|
| `explorer-recherche` | Retrouver une fiche par son nom ? | recherche globale de l'Explorer (nom + contenu) ; aussi via palette ⌘K | chercher, retrouver, nom, recherche |
| `explorer-filtres` | Filtrer par catégorie et sous-catégorie ? | rangées de catégories à cocher + chips ; volet de filtres fonctionnels ; les filtres se cumulent | filtre, catégorie, sous-catégorie, affiner |
| `explorer-vues` | Passer de la liste à la carte ou au tableau ? | vues Liste / Table / Carte / Split ; itinéraires tracés sur la carte | carte, liste, tableau, vue, affichage |
| `explorer-fiche` | Consulter une fiche sans la modifier ? | clic → panneau de détail (lecture seule) ; bouton Modifier → éditeur complet | consulter, détail, fiche, panneau |
| `explorer-partager-fiche` | Partager un lien direct vers une fiche ? | l'URL porte `?fiche=` : copier l'adresse suffit | partager, lien, url, envoyer |
| `explorer-brouillons` | Pourquoi je vois des fiches « brouillon » ? | brouillons de VOTRE organisation, visibles seulement si votre compte peut éditer ; jamais archivées/masquées ici | brouillon, draft, non publié, visible |
| `explorer-ouvert` | Que signifie la pastille « Ouvert » ? | calculée depuis les horaires saisis ; ABSENTE si horaires inconnus (jamais « fermé » par défaut) | ouvert, fermé, horaires, pastille |
| `explorer-imprimer` | Imprimer une sélection de fiches ? | sélection → impression cartes carnet ; pour des sélections durables → module Listes (le renvoi `related: ['listes-creer']` est AJOUTÉ en Task 7, quand la cible existe) | imprimer, sélection, carnet, pdf |

- [ ] **Step 3: Brancher dans `index.ts`** (`DEMARRER_FAQ` en tête, `EXPLORER_FAQ` après `CHOISIR_TYPE_FAQ`).

- [ ] **Step 4: Vérifier** — `npm run test:run -- content-integrity` → PASS (43 entrées).

- [ ] **Step 5: Commit**

```bash
git add src/features/help/content/demarrer.ts src/features/help/content/explorer.ts src/features/help/content/index.ts
git commit -m "feat(aide): corpus Demarrer & compte (7) + Explorer & filtres (8)" -- src/features/help/content/demarrer.ts src/features/help/content/explorer.ts src/features/help/content/index.ts
```

---

### Task 6 : Corpus « Éditer une fiche » (10) + « Publication & modération » (7)

**Files:**
- Create: `src/features/help/content/editeur.ts` (export `EDITEUR_FAQ`)
- Create: `src/features/help/content/publication.ts` (export `PUBLICATION_FAQ`)
- Modify: `src/features/help/content/index.ts`
- Modify: `src/features/help/content/creer-objet.ts` (ajouter `publier-fiche` aux `related` de `creer-fiche` — cible créée ici)

- [ ] **Step 1: Rédiger `editeur.ts`.** Sources : `src/features/object-editor/` (registre des sections — grepper les titres réels), CLAUDE.md (éditeur pleine page, §20 descriptions, pipeline média), mémoire §147/§113/§96/§92 :

| id | Question | Points imposés | Keywords |
|---|---|---|---|
| `editeur-ouvrir` | Modifier une fiche ? | bouton Modifier (Explorer/détail) → éditeur pleine page en sections ; barre d'enregistrement globale | modifier, éditer, changer, fiche |
| `editeur-sections` | À quoi correspondent les sections ? | sections numérotées adaptées AU TYPE (un restaurant a menus, un itinéraire a le tracé…) ; sections indisponibles = bandeau explicite | section, onglet, rubrique, éditeur |
| `editeur-enregistrer` | Enregistrer mes modifications ? | bouton global ; confirmation snackbar ; modules enregistrés ensemble | enregistrer, sauvegarder, save, valider |
| `editeur-descriptions` | Description : « canonique » vs « mon organisation » ? | canonique = commune ; overlay par organisation (champ vide → retombe sur la canonique) ; mise en forme Markdown (gras, listes, titres) | description, texte, présentation, mise en forme |
| `editeur-photos` | Ajouter des photos ou vidéos ? | section Médias : photos redimensionnées et nettoyées automatiquement (données EXIF retirées) ; vidéos mp4/webm/mov ≤ 100 Mo ; étoile = photo de couverture (pas de vidéo en couverture) ; visibilité par média | photo, image, vidéo, couverture, galerie |
| `editeur-horaires` | Saisir les horaires d'ouverture ? | périodes (saisons, récurrence annuelle), horaires par jour, fermetures exceptionnelles ; « ouvert sans horaires » possible ; alimente la pastille Ouvert | horaires, ouverture, fermeture, saison, jours |
| `editeur-blocages` | « Enregistrement bloqué » ou « publication bloquée » ? | fenêtre listant les blocages avec leur raison ; corriger puis réessayer | bloqué, erreur, impossible, publier |
| `editeur-module-indisponible` | « Module non disponible pour ce type » ? | certaines données n'existent que pour certains types (menus → restaurants…) ; si le type est faux → voir arbitrages (renvoi `choisir-artisan`) | indisponible, module, grisé, type |
| `editeur-presence` | Plusieurs personnes sur la même fiche ? | présence en temps réel : avatars + badge par section + bannière de conflit | conflit, simultané, présence, collègue |
| `editeur-zones` | Communes desservies (prestataires sans adresse fixe) ? | multi-sélection de communes dans Lieux & zones ; complète ou remplace l'adresse selon le cas | commune, zone, desservie, secteur |

- [ ] **Step 2: Rédiger `publication.ts`.** Sources : CLAUDE.md (write-path, hard delete §108, Explorer visibility), mémoire §120/§122, `ModerationPage.tsx` :

| id | Question | Points imposés | Keywords |
|---|---|---|---|
| `publier-fiche` | Publier une fiche ? | section Publication de l'éditeur ; confirmation explicite ; nécessite le droit de publication ; blocages affichés | publier, en ligne, visible, diffuser |
| `statuts-fiche` | Brouillon, publiée, masquée, archivée : qui voit quoi ? | brouillon = votre org (éditeurs) ; publiée = tous ; masquée/archivée = hors Explorer | statut, brouillon, publiée, archivée, masquée |
| `archiver-fiche` | Archiver une fiche (fermeture, saison finie) ? | section Publication ; réversible ; l'archivage retire de l'Explorer ; préalable à la suppression définitive | archiver, fermer, retirer, désactiver |
| `supprimer-fiche` | Supprimer définitivement une fiche ? | réservé superuser ; fiche archivée d'abord ; confirmation par saisie du nom exact ; IRRÉVERSIBLE (médias effacés) | supprimer, effacer, définitif, retirer |
| `moderation-suggestions` | Pourquoi mes modifications passent « en modération » ? | sans droit d'écriture canonique, l'enregistrement crée des suggestions soumises aux modérateurs ; suivi du statut | modération, suggestion, attente, validation |
| `moderer` | Approuver ou rejeter des suggestions ? | module Modération : diff avant/après, approuver ou rejeter avec motif obligatoire ; badge de compteur dans le menu | modérer, approuver, rejeter, suggestions |
| `signaler-doublon` | J'ai repéré un doublon publié ? | ne pas supprimer soi-même ; signaler à l'OTI (arbitrage fusion/archivage) ; renvoi `choisir-doublon` | doublon, deux fois, fusion, signaler |

- [ ] **Step 3: Brancher dans `index.ts`**, ordre : après `EXPLORER_FAQ`.

- [ ] **Step 4: Vérifier** — `npm run test:run -- content-integrity` → PASS (60 entrées).

- [ ] **Step 5: Commit**

```bash
git add src/features/help/content/editeur.ts src/features/help/content/publication.ts src/features/help/content/index.ts
git commit -m "feat(aide): corpus Editer une fiche (10) + Publication & moderation (7)" -- src/features/help/content/editeur.ts src/features/help/content/publication.ts src/features/help/content/index.ts
```

---

### Task 7 : Corpus « Listes » (5) + « CRM » (6) + « Équipe » (6) + « Réglages & RGPD » (6) + test de qualité de recherche sur corpus réel

**Files:**
- Create: `src/features/help/content/listes.ts`, `crm.ts`, `equipe.ts`, `reglages.ts`
- Modify: `src/features/help/content/index.ts`
- Modify: `src/features/help/content/explorer.ts` (ajouter `related: ['listes-creer']` à `explorer-imprimer` — cible créée ici)
- Test: `src/features/help/faq-search.corpus.test.ts`

- [ ] **Step 1: Rédiger les 4 fichiers.** Tableaux (sources : mémoire §134-148 listes, §61/§63 CRM, §164 équipe, §166/§149 réglages, `TeamAdminPage.tsx`, `SettingsPage.tsx`, `RgpdErasurePage.tsx`) :

`listes.ts` :

| id | Question | Points imposés | Keywords |
|---|---|---|---|
| `listes-creer` | Créer une liste de fiches ? | module Listes ; nom + modèle ; sélections durables réutilisables | liste, sélection, créer, regrouper |
| `listes-ajouter` | Ajouter des fiches à une liste ? | palette d'ajout avec recherche ; enregistrement automatique à l'ajout | ajouter, liste, fiche, palette |
| `listes-imprimer` | Imprimer une liste (carnet) ? | rendu cartes carnet, pied de page numéroté ; impression navigateur | imprimer, carnet, pdf, papier |
| `listes-carte` | La carte récapitulative d'une liste ? | carte réelle avec les fiches localisées ; incluse à l'impression | carte, récap, points, localisation |
| `listes-partager` | Partager ou envoyer une liste ? | décrire UNIQUEMENT ce qui existe — vérifier `ListsManageView.tsx`/`ListComposeView.tsx` (partage/envoi) avant rédaction ; sinon réponse = impression + remise en main propre | partager, envoyer, transmettre, lien |

`crm.ts` :

| id | Question | Points imposés | Keywords |
|---|---|---|---|
| `crm-quoi` | À quoi sert le module CRM ? | suivi RELATIONNEL centré sur les personnes (acteurs) : interactions, tâches, annuaire — distinct des fiches | crm, suivi, relation, socio-pro |
| `crm-acteur-vs-fiche` | Acteur ou fiche : quelle différence ? | fiche = l'offre publiée ; acteur = la personne/l'exploitant ; un acteur peut être lié à plusieurs fiches ; renvoi `choisir-org-actor` | acteur, contact, personne, gérant |
| `crm-interaction` | Enregistrer un échange (appel, visite, e-mail) ? | interaction ancrée sur l'acteur, contexte fiche optionnel ; sujet (20 sujets OTI) + ressenti | interaction, appel, visite, échange, note |
| `crm-taches` | Créer et suivre des tâches ? | kanban 3 statuts, échéances, rattachement acteur | tâche, kanban, à faire, échéance, relance |
| `crm-annuaire` | Retrouver un socio-professionnel ? | annuaire filtrable (sujet, statut, période) ; fiche acteur : coordonnées, identité, historique | annuaire, socio-pro, coordonnées, téléphone |
| `crm-nouvel-acteur` | Créer un acteur ou corriger ses coordonnées ? | création + édition identité/canaux depuis le CRM ; les coordonnées de l'acteur ≠ contacts publics de la fiche | nouvel acteur, coordonnées, corriger, e-mail |

`equipe.ts` :

| id | Question | Points imposés | Keywords |
|---|---|---|---|
| `equipe-ou` | Où gérer mon équipe ? | Réglages → Mon organisation (plus d'entrée menu dédiée) | équipe, membres, organisation, gérer |
| `equipe-inviter` | Inviter un ou plusieurs collègues ? | invitation par e-mail ; plusieurs adresses d'un coup (une par ligne/virgule) ; même rôle appliqué au lot ; récap par adresse | inviter, collègue, e-mail, ajouter, compte |
| `equipe-roles` | Quels rôles et permissions ? | rôles métier + admin ; permissions d'édition/publication ; décrire les rôles RÉELS de TeamAdminPage — vérifier avant rédaction | rôle, permission, droits, admin |
| `equipe-renvoyer` | Renvoyer une invitation restée sans réponse ? | bouton Renvoyer pour les comptes en attente jamais connectés | renvoyer, invitation, attente, relance |
| `equipe-supprimer` | Retirer un compte de l'organisation ? | suppression définitive par l'admin ; impossible sur soi-même | supprimer, retirer, départ, compte |
| `equipe-mdp-collegue` | Un collègue a perdu son mot de passe ? | il utilise « Mot de passe oublié » sur l'écran de connexion (renvoi `mot-de-passe-oublie`) ; l'admin peut aussi ré-inviter un compte jamais activé | mot de passe, collègue, perdu, aider |

`reglages.ts` :

| id | Question | Points imposés | Keywords |
|---|---|---|---|
| `reglages-branding` | Logo et couleurs de mon organisation ? | Réglages → branding : logo (transparence conservée) + couleurs du thème appliqués à toute l'app | logo, couleur, thème, marque, branding |
| `reglages-compte` | Gérer mon compte (nom, photo, mot de passe) ? | Réglages → Mon compte ; renvoi `profil-avatar` et `mot-de-passe-oublie` | compte, profil, préférences |
| `reglages-referentiels` | Modifier les listes de valeurs (référentiels) ? | console d'administration réservée superuser ; compteur d'usage, suppression seulement à 0 usage | référentiel, valeur, liste, vocabulaire |
| `rgpd-droits` | Traiter une demande RGPD (effacement) ? | module RGPD réservé owner/super_admin ; effacement des données personnelles d'un acteur | rgpd, effacement, données personnelles, droit |
| `aide-partenaires` | Un guide pour nos partenaires socio-pros ? | guide partenaires publié sur le site documentaire de l'OTI (espace partenaires) | guide, partenaire, documentation, socio-pro |
| `aide-contact` | Je ne trouve pas ma réponse ici ? | contacter l'administrateur de votre organisation ou l'équipe Bertel de l'OTI du Sud ; la FAQ s'enrichit — signaler les questions manquantes | contact, support, question, manquant |

- [ ] **Step 2: Brancher dans `index.ts`** — ordre final ALL_FAQ_ENTRIES = DEMARRER, CREER_OBJET, CHOISIR_TYPE, EXPLORER, EDITEUR, PUBLICATION, LISTES, CRM, EQUIPE, REGLAGES.

- [ ] **Step 3: Écrire le test de qualité de recherche sur le corpus réel**

```ts
// src/features/help/faq-search.corpus.test.ts
/** Garde de QUALITÉ de la recherche sur le corpus réel : les mots métier phares
 *  doivent remonter la bonne entrée en tête. Si un remaniement du contenu casse
 *  un de ces cas, c'est une régression produit, pas un détail. */
import { ALL_FAQ_ENTRIES } from './content';
import { searchFaq } from './faq-search';

function topIds(query: string, n = 3): string[] {
  return searchFaq(ALL_FAQ_ENTRIES, query).slice(0, n).map((e) => e.id);
}

describe('recherche métier sur le corpus réel', () => {
  test('« artisan » → l’arbitrage artisan en premier', () => {
    expect(topIds('artisan')[0]).toBe('choisir-artisan');
  });

  test('« artis » (préfixe) trouve aussi l’arbitrage artisan', () => {
    expect(topIds('artis')).toContain('choisir-artisan');
  });

  test('« gîte » → la fiche création HLO dans le top 3', () => {
    expect(topIds('gîte')).toContain('creer-hlo');
  });

  test('« atelier » → l’arbitrage artisan ET la création ACT dans le top 3', () => {
    const ids = topIds('atelier');
    expect(ids).toContain('choisir-artisan');
    expect(ids).toContain('creer-act');
  });

  test('« publier » → publier-fiche en premier', () => {
    expect(topIds('publier')[0]).toBe('publier-fiche');
  });

  test('« mot de passe » → la réinitialisation dans le top 3', () => {
    expect(topIds('mot de passe')).toContain('mot-de-passe-oublie');
  });

  test('« marché » → l’arbitrage marché dans le top 3', () => {
    expect(topIds('marché')).toContain('choisir-marche');
  });
});
```

- [ ] **Step 4: Vérifier** — `npm run test:run -- features/help` → PASS intégral (intégrité 83 entrées + corpus). Si un cas de recherche échoue : **ajuster les keywords de l'entrée visée**, pas l'algorithme.

- [ ] **Step 5: Commit**

```bash
git add src/features/help/content/listes.ts src/features/help/content/crm.ts src/features/help/content/equipe.ts src/features/help/content/reglages.ts src/features/help/content/index.ts src/features/help/faq-search.corpus.test.ts
git commit -m "feat(aide): corpus Listes (5) + CRM (6) + Equipe (6) + Reglages & RGPD (6) — 83 entrees ; garde de qualite recherche sur corpus reel (artisan/gite/atelier/publier...)" -- src/features/help/content/listes.ts src/features/help/content/crm.ts src/features/help/content/equipe.ts src/features/help/content/reglages.ts src/features/help/content/index.ts src/features/help/faq-search.corpus.test.ts
```

---

### Task 8 : Page `/aide` (HelpPage + route + styles)

**Files:**
- Create: `src/views/HelpPage.tsx`
- Create: `src/app/(main)/aide/page.tsx`
- Modify: `src/styles.css` (famille `.help-*` en fin de fichier)
- Test: `src/views/HelpPage.test.tsx`

**Interfaces:**
- Consumes: `ALL_FAQ_ENTRIES`, `FAQ_RUBRIQUES`, `FaqEntry`, `FaqRubriqueId`, `searchFaq`, `MarkdownContent({ markdown, className })`, `createTypeLabel(code)` de `create/create-object-options.ts`, `cn` de `@/lib/utils`.
- Produces: default export `HelpPage` ; route `/aide`.

- [ ] **Step 1: Écrire le test RTL qui échoue**

```tsx
// src/views/HelpPage.test.tsx
import { render, screen, fireEvent } from '@testing-library/react';
import HelpPage from './HelpPage';

const replace = jest.fn();
let searchParams = new URLSearchParams();
jest.mock('next/navigation', () => ({
  useRouter: () => ({ replace }),
  useSearchParams: () => searchParams,
}));

describe('HelpPage', () => {
  beforeEach(() => {
    searchParams = new URLSearchParams();
    replace.mockClear();
  });

  test('rend les rubriques et les questions en accordéon fermé', () => {
    render(<HelpPage />);
    expect(screen.getByRole('heading', { name: 'Aide' })).toBeInTheDocument();
    // getByRole heading (le chip de rubrique porte le même libellé → getByText serait ambigu)
    expect(screen.getByRole('heading', { name: 'Créer une fiche' })).toBeInTheDocument();
    const artisan = screen.getByRole('button', { name: /Je veux créer un artisan/ });
    expect(artisan).toHaveAttribute('aria-expanded', 'false');
  });

  test('la recherche « artisan » remonte l’arbitrage en premier résultat', async () => {
    render(<HelpPage />);
    fireEvent.change(screen.getByLabelText("Rechercher dans l'aide"), {
      target: { value: 'artisan' },
    });
    // debounce 150 ms → findBy* attend
    const status = await screen.findByRole('status');
    expect(status.textContent).toMatch(/résultat/);
    const questions = screen.getAllByRole('button', { name: /./ })
      .filter((b) => b.className.includes('help-qa__question'));
    expect(questions[0]).toHaveAccessibleName(/Je veux créer un artisan/);
  });

  test('état vide : message + suggestion', async () => {
    render(<HelpPage />);
    fireEvent.change(screen.getByLabelText("Rechercher dans l'aide"), {
      target: { value: 'zzzzzzz' },
    });
    expect(await screen.findByText(/Aucun résultat pour/)).toBeInTheDocument();
  });

  test('deep-link ?question= ouvre l’entrée ciblée', () => {
    searchParams = new URLSearchParams('question=choisir-artisan');
    render(<HelpPage />);
    expect(screen.getByRole('button', { name: /Je veux créer un artisan/ }))
      .toHaveAttribute('aria-expanded', 'true');
    expect(screen.getByText(/ce que le visiteur vient faire/)).toBeInTheDocument();
  });

  test('deep-link ?q= préremplit la recherche', async () => {
    searchParams = new URLSearchParams('q=artisan');
    render(<HelpPage />);
    expect(screen.getByLabelText("Rechercher dans l'aide")).toHaveValue('artisan');
    expect(await screen.findByRole('status')).toBeInTheDocument();
  });

  test('ouvrir une entrée synchronise l’URL (replace, pas push)', () => {
    render(<HelpPage />);
    fireEvent.click(screen.getByRole('button', { name: /Je veux créer un artisan/ }));
    expect(replace).toHaveBeenCalledWith('/aide?question=choisir-artisan');
  });
});
```

- [ ] **Step 2: Vérifier l'échec** — `npm run test:run -- HelpPage` → FAIL (module inexistant).

- [ ] **Step 3: Implémenter `HelpPage.tsx`**

```tsx
// src/views/HelpPage.tsx
'use client';

// Centre d'aide /aide (spec 2026-07-03-faq-aide-design.md) : recherche métier instantanée
// (searchFaq — accents/préfixes/keywords), rubriques en chips, questions en accordéon,
// deep-links ?question=<id> (ouvre + scrolle) et ?q=<texte> (préremplit la recherche).
import { useEffect, useMemo, useRef, useState } from 'react';
import { useRouter, useSearchParams } from 'next/navigation';
import { ChevronDown, Search } from 'lucide-react';
import { MarkdownContent } from '../components/markdown/MarkdownContent';
import { ALL_FAQ_ENTRIES } from '../features/help/content';
import { FAQ_RUBRIQUES, type FaqEntry, type FaqRubriqueId } from '../features/help/content/types';
import { searchFaq } from '../features/help/faq-search';
import { createTypeLabel } from '../features/object-editor/create/create-object-options';
import { cn } from '@/lib/utils';

const SEARCH_DEBOUNCE_MS = 150;

const RUBRIQUE_LABEL = new Map<string, string>(FAQ_RUBRIQUES.map((r) => [r.id, r.label]));
const ENTRY_BY_ID = new Map(ALL_FAQ_ENTRIES.map((e) => [e.id, e]));

function FaqEntryCard({
  entry,
  open,
  showRubrique,
  onToggle,
  onOpenRelated,
}: {
  entry: FaqEntry;
  open: boolean;
  showRubrique: boolean;
  onToggle: () => void;
  onOpenRelated: (id: string) => void;
}) {
  const related = (entry.related ?? [])
    .map((id) => ENTRY_BY_ID.get(id))
    .filter((e): e is FaqEntry => Boolean(e));
  return (
    <article id={`faq-${entry.id}`} className={cn('help-qa', open && 'help-qa--open')}>
      <button type="button" className="help-qa__question" aria-expanded={open} onClick={onToggle}>
        <span className="help-qa__text">{entry.question}</span>
        {showRubrique && (
          <span className="help-qa__rubrique">{RUBRIQUE_LABEL.get(entry.rubrique)}</span>
        )}
        {(entry.types ?? []).map((code) => (
          <span key={code} className="help-qa__badge">{createTypeLabel(code)}</span>
        ))}
        <ChevronDown className="help-qa__chevron" aria-hidden />
      </button>
      {open && (
        <div className="help-qa__answer">
          <MarkdownContent markdown={entry.answer} />
          {related.length > 0 && (
            <p className="help-qa__related">
              <span>Voir aussi :</span>
              {related.map((r) => (
                <button key={r.id} type="button" onClick={() => onOpenRelated(r.id)}>
                  {r.question}
                </button>
              ))}
            </p>
          )}
        </div>
      )}
    </article>
  );
}

export default function HelpPage() {
  const router = useRouter();
  const searchParams = useSearchParams();
  const [query, setQuery] = useState(() => searchParams.get('q') ?? '');
  const [debouncedQuery, setDebouncedQuery] = useState(query);
  const [openId, setOpenId] = useState<string | null>(() => searchParams.get('question'));
  const [rubrique, setRubrique] = useState<FaqRubriqueId | 'all'>('all');
  const didInitialScroll = useRef(false);

  useEffect(() => {
    const id = setTimeout(() => setDebouncedQuery(query), SEARCH_DEBOUNCE_MS);
    return () => clearTimeout(id);
  }, [query]);

  // Deep-link entrant : scroller UNE fois vers l'entrée ouverte par ?question=.
  useEffect(() => {
    if (didInitialScroll.current || !openId) return;
    didInitialScroll.current = true;
    document.getElementById(`faq-${openId}`)?.scrollIntoView?.({ block: 'start' });
  }, [openId]);

  function toggleEntry(id: string) {
    const next = openId === id ? null : id;
    setOpenId(next);
    // URL partageable, sans empiler l'historique.
    router.replace(next ? `/aide?question=${next}` : '/aide');
  }

  function openRelated(id: string) {
    setOpenId(id);
    router.replace(`/aide?question=${id}`);
    document.getElementById(`faq-${id}`)?.scrollIntoView?.({ block: 'start' });
  }

  const results = useMemo(
    () => (debouncedQuery.trim() ? searchFaq(ALL_FAQ_ENTRIES, debouncedQuery) : null),
    [debouncedQuery],
  );

  const sections = FAQ_RUBRIQUES.filter((r) => rubrique === 'all' || r.id === rubrique).map(
    (r) => ({ ...r, entries: ALL_FAQ_ENTRIES.filter((e) => e.rubrique === r.id) }),
  );

  return (
    <div className="help-app">
      <header className="help-head">
        <h1>Aide</h1>
        <p className="help-head__sub">
          Cherchez un mot du métier : « artisan », « gîte », « atelier », « publier »…
        </p>
        <div className="help-search">
          <Search className="help-search__icon" aria-hidden />
          <input
            type="search"
            autoFocus
            value={query}
            onChange={(event) => setQuery(event.target.value)}
            placeholder="Poser une question ou taper un mot-clé…"
            aria-label="Rechercher dans l'aide"
          />
        </div>
      </header>

      {results ? (
        <section className="help-results" aria-label="Résultats de recherche">
          <p className="help-count" role="status">
            {results.length} résultat{results.length > 1 ? 's' : ''}
          </p>
          {results.length === 0 ? (
            <div className="help-empty">
              <p>Aucun résultat pour « {debouncedQuery.trim()} ».</p>
              <p>Essayez un mot plus court ou un synonyme métier (« artisan », « gîte », « atelier »…).</p>
            </div>
          ) : (
            results.map((entry) => (
              <FaqEntryCard
                key={entry.id}
                entry={entry}
                open={openId === entry.id}
                showRubrique
                onToggle={() => toggleEntry(entry.id)}
                onOpenRelated={openRelated}
              />
            ))
          )}
        </section>
      ) : (
        <>
          {/* Chips de filtre (pas des onglets ARIA : pas de role tablist sans tabs) */}
          <div className="help-chips" aria-label="Rubriques">
            <button
              type="button"
              className={cn('help-chip', rubrique === 'all' && 'help-chip--on')}
              onClick={() => setRubrique('all')}
            >
              Toutes
            </button>
            {FAQ_RUBRIQUES.map((r) => (
              <button
                key={r.id}
                type="button"
                className={cn('help-chip', rubrique === r.id && 'help-chip--on')}
                onClick={() => setRubrique(rubrique === r.id ? 'all' : r.id)}
              >
                {r.label}
              </button>
            ))}
          </div>
          {sections.map((section) => (
            <section key={section.id} className="help-section" aria-label={section.label}>
              <h2 className="help-section__title">{section.label}</h2>
              {section.entries.map((entry) => (
                <FaqEntryCard
                  key={entry.id}
                  entry={entry}
                  open={openId === entry.id}
                  showRubrique={false}
                  onToggle={() => toggleEntry(entry.id)}
                  onOpenRelated={openRelated}
                />
              ))}
            </section>
          ))}
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 4: Créer la route**

```tsx
// src/app/(main)/aide/page.tsx
'use client';

import { Suspense } from 'react';
import HelpPage from '@/views/HelpPage';

// useSearchParams exige une frontière Suspense côté App Router.
export default function AidePage() {
  return (
    <Suspense fallback={null}>
      <HelpPage />
    </Suspense>
  );
}
```

- [ ] **Step 5: Styles — ajouter en FIN de `src/styles.css`** (tokens vérifiés : `--ink`, `--ink-3`, `--line`, `--line-strong`, `--surface`, `--bg-tint`, `--teal`, `--teal-soft`) :

```css
/* ============================================================
   /aide — centre d'aide (FAQ). Scopé .help-* (pattern .crm-app).
   ============================================================ */
.help-app { max-width: 860px; margin: 0 auto; padding: 24px 20px 64px; }
.help-head h1 { font-size: 1.5rem; font-weight: 700; color: var(--ink); margin: 0 0 4px; }
.help-head__sub { color: var(--ink-3); margin: 0 0 16px; font-size: 0.925rem; }
.help-search { position: relative; margin-bottom: 20px; }
.help-search__icon { position: absolute; left: 12px; top: 50%; transform: translateY(-50%); width: 18px; height: 18px; color: var(--ink-3); pointer-events: none; }
.help-search input {
  width: 100%; padding: 12px 14px 12px 40px; font-size: 1rem;
  border: 1px solid var(--line-strong); border-radius: 12px;
  background: var(--surface); color: var(--ink);
}
.help-chips { display: flex; flex-wrap: wrap; gap: 8px; margin-bottom: 20px; }
.help-chip {
  padding: 6px 12px; border-radius: 999px; font-size: 0.85rem;
  border: 1px solid var(--line-strong); background: var(--surface); color: var(--ink);
  cursor: pointer;
}
.help-chip--on { background: var(--teal); border-color: var(--teal); color: #fff; }
.help-section { margin-bottom: 24px; }
.help-section__title { font-size: 1.05rem; font-weight: 600; color: var(--ink); margin: 0 0 10px; }
.help-count { color: var(--ink-3); font-size: 0.875rem; margin: 0 0 12px; }
.help-qa { border: 1px solid var(--line); border-radius: 12px; background: var(--surface); margin-bottom: 8px; }
.help-qa--open { border-color: var(--line-strong); }
.help-qa__question {
  display: flex; align-items: center; gap: 8px; width: 100%; text-align: left;
  padding: 12px 14px; background: none; border: 0; cursor: pointer;
  font-size: 0.95rem; font-weight: 600; color: var(--ink);
}
.help-qa__text { flex: 1; }
.help-qa__rubrique { font-size: 0.75rem; font-weight: 500; color: var(--ink-3); white-space: nowrap; }
.help-qa__badge {
  font-size: 0.72rem; font-weight: 600; padding: 2px 8px; border-radius: 999px;
  background: var(--teal-soft); color: var(--teal); white-space: nowrap;
}
.help-qa__chevron { width: 16px; height: 16px; color: var(--ink-3); flex-shrink: 0; transition: transform 150ms ease; }
.help-qa--open .help-qa__chevron { transform: rotate(180deg); }
.help-qa__answer { padding: 0 14px 14px; border-top: 1px solid var(--line); padding-top: 12px; }
.help-qa__related { display: flex; flex-wrap: wrap; gap: 6px; align-items: center; margin: 12px 0 0; font-size: 0.85rem; color: var(--ink-3); }
.help-qa__related button {
  border: 1px solid var(--line); border-radius: 999px; background: var(--bg-tint);
  padding: 3px 10px; font-size: 0.8rem; color: var(--ink); cursor: pointer;
}
.help-qa__related button:hover { border-color: var(--line-strong); }
.help-empty { border: 1px dashed var(--line-strong); border-radius: 12px; padding: 24px; text-align: center; color: var(--ink-3); }
.help-empty p { margin: 4px 0; }
```

- [ ] **Step 6: Vérifier** — `npm run test:run -- HelpPage` → PASS (6 tests) ; `npm run typecheck` → 0 erreur.

- [ ] **Step 7: Commit**

```bash
git add src/views/HelpPage.tsx src/views/HelpPage.test.tsx "src/app/(main)/aide/page.tsx" src/styles.css
git commit -m "feat(aide): page /aide — recherche instantanee (debounce 150ms), chips rubriques, accordeon MarkdownContent, badges types, Voir aussi, deep-links ?question=/?q=" -- src/views/HelpPage.tsx src/views/HelpPage.test.tsx "src/app/(main)/aide/page.tsx" src/styles.css
```

---

### Task 9 : Navigation — bouton sidebar + NAV_ITEMS (palette ⌘K)

**Files:**
- Modify: `src/config/nav-items.ts`
- Modify: `src/components/layout/Sidebar.tsx:43` (filtre) et `:116-122` (footer)

**Interfaces:**
- Consumes: route `/aide` (Task 8), `CircleHelp` (déjà importé dans Sidebar), `isActivePath` (locale à Sidebar).
- Produces: entrée NAV_ITEMS `/aide` (la palette ⌘K la liste automatiquement via `visibleNavItems`).

- [ ] **Step 1: Vérifier le demo-gating** — `grep -n "isDemoOnlyModule" src/utils/features.ts` : confirmer que `/aide` n'est PAS dans la liste demo-only (sinon l'y soustraire explicitement).

- [ ] **Step 2: `nav-items.ts`** — ajouter `CircleHelp` à l'import lucide et l'entrée en DERNIER du tableau :

```ts
  // /aide est rendu par le FOOTER de la Sidebar (bouton Aide) — la boucle principale
  // l'exclut (comme /settings) ; l'entrée existe ici pour la palette ⌘K (registre unique D24).
  { to: '/aide', label: 'Aide', caption: 'FAQ et centre d’aide', roles: ['owner', 'super_admin', 'tourism_agent'], icon: CircleHelp },
```

- [ ] **Step 3: `Sidebar.tsx`** — ligne 43, exclure `/aide` de la boucle principale :

```ts
  const navItems = items.filter((item) => item.to !== '/settings' && item.to !== '/aide');
```

et remplacer le bouton mort du footer (lignes 117-122) par :

```tsx
          <Link
            href="/aide"
            title="Aide"
            aria-current={isActivePath(pathname, '/aide') ? 'page' : undefined}
            className={cn('app-sidebar__item', isActivePath(pathname, '/aide') && 'app-sidebar__item--active')}
          >
            <span className="app-sidebar__iconbox">
              <CircleHelp className="app-sidebar__icon" strokeWidth={1.8} aria-hidden />
            </span>
            <span className="app-sidebar__label">Aide</span>
          </Link>
```

- [ ] **Step 4: Vérifier les tests existants** — `npm run test:run -- CommandPalette` (le test enumère peut-être les entrées nav : s'il échoue sur le compte, mettre à jour l'attendu avec l'entrée Aide) puis `npm run test:run -- Sidebar` (s'il existe). `npm run typecheck` → 0 erreur.

- [ ] **Step 5: Vérification navigateur (preview)** — démarrer le dev server (preview_start), aller sur `/aide` : la page rend, la recherche « artisan » remonte l'arbitrage, le bouton Aide de la sidebar est actif sur /aide, la palette ⌘K propose « Aide ». Screenshot en preuve.

- [ ] **Step 6: Commit**

```bash
git add src/config/nav-items.ts src/components/layout/Sidebar.tsx
git commit -m "feat(aide): bouton Aide sidebar branche sur /aide (Link + etat actif) ; entree NAV_ITEMS pour la palette (exclue de la boucle principale, rendue par le footer)" -- src/config/nav-items.ts src/components/layout/Sidebar.tsx
```

---

### Task 10 : Passe finale — suites complètes, graphe, journal de décisions

**Files:**
- Modify: `bertel-tourism-ui/claude_brief/lot1_mapping_decisions.md` (nouvelle section §N)
- Modify: `.claude/WORKFLOW.md` si un différé est acté

- [ ] **Step 1: Suites complètes** — `npm run test:run` (tout Jest) et `npm run typecheck` → 0 échec. Corriger avant de continuer.

- [ ] **Step 2: Relecture contenu anti-invention** — repasser les 83 entrées avec ce filtre : chaque phrase décrivant un COMPORTEMENT de l'app doit être rattachable à une source du tableau « bible ». Supprimer ou reformuler toute phrase invérifiable. Vérifier en particulier : `choisir-marche` (règle §57 exacte), `equipe-roles` (rôles réels), `listes-partager` (n'affirmer que l'existant), `creer-psv` (périmètre PSV).

- [ ] **Step 3: Rafraîchir le graphe** — depuis la racine du repo : `graphify update .`

- [ ] **Step 4: Journal de décisions** — re-grepper le DERNIER numéro `## §` de `bertel-tourism-ui/claude_brief/lot1_mapping_decisions.md` (gotcha collision : ne pas se fier au souvenir), puis ajouter la section §N : décisions (contenu repo vs DB, page vs modale, `related` hors Markdown à cause de SafeLink, scoring keywords>question>réponse), invariant de couverture TYPE_ARCHETYPES par le test d'intégrité, différés (édition DB du contenu, aide contextuelle par écran `?question=`, captures d'écran, i18n).

- [ ] **Step 5: Commit final**

```bash
git add bertel-tourism-ui/claude_brief/lot1_mapping_decisions.md
git commit -m "docs(aide): decision log SN — FAQ /aide livree (83 entrees, invariant couverture types, differes DB/contextuel/i18n)" -- bertel-tourism-ui/claude_brief/lot1_mapping_decisions.md
```

(Remplacer `SN` par le numéro réel. Si `graphify update` a modifié `graphify-out/`, le committer séparément en `chore(graph)`.)

---

## Récapitulatif des livrables

| # | Livrable | Test qui le garde |
|---|---|---|
| 1 | `FaqEntry` + 10 rubriques | `types.test.ts` |
| 2 | `searchFaq` (accents, préfixes, scoring) | `faq-search.test.ts` (8 cas) |
| 3-7 | 83 entrées, 10 fichiers de contenu | `content-integrity.test.ts` + `faq-search.corpus.test.ts` |
| 8 | Page `/aide` + styles + deep-links | `HelpPage.test.tsx` (6 cas) |
| 9 | Sidebar + palette ⌘K | tests existants CommandPalette/Sidebar + preview |
| 10 | Journal §N + graphe + suites vertes | `npm run test:run` intégral |
